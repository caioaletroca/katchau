"use client";

import createTheme from "@mui/material/styles/createTheme";

const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#B784FA",
        }
    }
});

export default theme;